<h1>Discography</h1>

<table class="table table-striped" border="1" align=center style="padding-right:20px; padding-left:20px;">
  <thead>
    <tr>
      <th>タイトル</th>
      <th colspan="3">操作</th>
    </tr>
  </thead>

  <tbody>
    <% @discographys.each do |disc| %>
      <tr>
        <td><%= disc.title %></td>
        <td><%= link_to '詳細', admin_discography_path(disc) %></td>
        <td><%= link_to '編集', edit_admin_discography_path(disc) %></td>
        <td><%= link_to '削除', admin_discography_path(disc), method: :delete, data: { confirm: '本当に削除しますか？データの復旧はできません。' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規作成', new_admin_discography_path %> |
<%= link_to '編集用TOPへ', "#" %>
