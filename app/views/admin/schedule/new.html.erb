<div style="text-align:center;" >
<h1>New Schedule</h1>

<%= simple_form_for [:admin, @event], wrapper: :horizontal_form, url: admin_schedule_index_path, html: { class: 'form-horizontal', role: true } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>
<%= link_to 'Back', admin_schedule_index_path %>
</div>
