<div style="text-align:center;padding:25px;">
  <% if @disc.artwork? %>
    <%= link_to image_tag(@disc.artwork.url, size: "300x300"), @disc.url, { target: "_blank" } %>
  <% end %>

  <table class="table table-striped table-hover itemInfo">
    <tbody>
      <tr>
        <td><strong>Title</strong></td>
        <td><%= @disc.title %></td>
      </tr>
      <tr>
        <td><strong>Release date</strong></td>
        <td><%= @disc.released_on %></td>
      </tr>
      <tr>
        <td><strong>Label</strong></td>
        <td><%= @disc.record_label %></td>
      </tr>
      <tr>
        <td><strong>Artwork by</strong></td>
        <td><%= @disc.artworked_by %></td>
      </tr>
      <tr>
        <td><strong>Format</strong></td>
        <td><%= FormatType.find(@disc.format).type %></td>
      </tr>
      <tr>
        <td><strong>Release Type</strong></td>
        <td><%= ReleaseType.find(@disc.release_type).type %></td>
      </tr>
      <tr>
        <td><strong>URL</strong></td>
        <td><%= link_to @disc.url, @disc.url, { target: "_blank" } %></td>
      </tr>
      <tr>
        <td><strong>Track List</strong></td>
        <td><%= simple_format(@disc.track_list) %></td>
      </tr>
    </tbody>
  </table>
</div>
